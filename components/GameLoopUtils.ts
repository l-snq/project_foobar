import * as THREE from "three";
import RAPIER from "@dimforge/rapier3d-compat";
import { Game } from "./GameManager";

export function updateLocalPlayer(dt: number, input: THREE.Vector3, camera: THREE.OrthographicCamera) {
}
